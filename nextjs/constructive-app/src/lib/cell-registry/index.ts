import { CellRegistry } from './registry';

// import { DEFAULT_CELLS } from './default-cells';

// Initialize the registry with default PostgreSQL cell types
// CellRegistry.registerBatch(DEFAULT_CELLS);

// Export everything
export * from './types';
export * from './registry';
// export * from './default-cells';
export { CellRegistry };
